# Runtime state
debug/
downloads/
file-history/
ide/
plans/
projects/
session-env/
shell-snapshots/
statsig/
todos/

# History (may contain sensitive data)
history.jsonl

# Plugins have their own git repo
plugins/

# Local overrides (not for sharing)
*.local.json
*.local.md
.claude.json
